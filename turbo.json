{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "cache": false,
      "outputs": ["dist/**", "typechain/**"]
    },
    "test": {
      "cache": false
    }
  }
}
