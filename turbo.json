{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "cache": true,
      "outputs": ["dist/**", "typechain/**", "artifacts/**", "out/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "cache": false
    }
  }
}
