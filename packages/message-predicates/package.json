{
  "name": "@fuel-bridge/message-predicates",
  "private": true,
  "main": "./dist/index.ts",
  "scripts": {
    "test": "cargo test",
    "check": "forc fmt --check && cargo fmt --check && cargo clippy --all-features --all-targets -- -D warnings",
    "format": "forc fmt && cargo fmt",
    "build": "forc build && cargo run && node ./scripts/build.js"
  },
  "devDependencies": {
    "fuels": "0.47.0"
  }
}
