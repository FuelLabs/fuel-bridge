{
  "name": "@fuel-bridge/fungible-token",
  "private": true,
  "main": "dist/index.ts",
  "scripts": {
    "format": "forc fmt && cargo fmt",
    "check": "forc fmt --check && cargo fmt --check && cargo clippy",
    "build": "forc build && node ./scripts/build.js",
    "test": "cargo test"
  },
  "dependencies": {
    "fuels": "0.0.0-next-20230707184416"
  }
}
